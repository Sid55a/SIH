import React from "react";
import Login from "@/components/Login";

export const metadata = {
  title: "Login",
};

function Page() {
  return (
    <div>
      <Login />
    </div>
  );
}

export default Page;
